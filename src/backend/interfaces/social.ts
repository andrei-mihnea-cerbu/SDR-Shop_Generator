export interface SocialRow {
  id: string;
  name: string;
  description: string;
  artistId: string;
  url: string;
}

export interface SocialEntity {
  id: string;
  name: string;
  description: string;
  url: string;
}

export interface SocialApiDto {
  id: string;
  name: string;
  description: string;
  url: string;
}
